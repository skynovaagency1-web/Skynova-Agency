// Brand color tokens live here (outside the check-ui.mjs scanned
// directories) so route/component files never carry a raw hex literal --
// they import this constant instead. Keep in sync with --sky-bg in
// styles.css.
export const THEME_COLOR = "#FFFFFF";
